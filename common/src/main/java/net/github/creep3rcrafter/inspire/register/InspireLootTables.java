package net.github.creep3rcrafter.inspire.register;

public class InspireLootTables {
    static {
        /*
        LootEvent.MODIFY_LOOT_TABLE.register((lootTables, id, context, builtin) -> {
            if (builtin && BuiltInLootTables.ANCIENT_CITY.equals(id)){
                LootPool.Builder pool = LootPool.lootPool()
                        .add(LootItem.lootTableItem(InspireItems.ECHO_ARROW.get()).setWeight(1))
                        .add(LootItem.lootTableItem(InspireItems.AMETHYST_ARROW.get()).setWeight(1))
                        .add(LootItem.lootTableItem(InspireItems.BONE_ARROW.get()).setWeight(2))
                        .add(EmptyLootItem.emptyItem().setWeight(1))
                        .setRolls(UniformGenerator.between(0, 3));
                context.addPool(pool);
            }
        });
         */
    }
}
