plugins {
    id 'com.gradleup.shadow'
}
architectury {
    platformSetupLoomIde()
    fabric()
}
fabricApi {
    configureDataGeneration() {
        client = true
    }
}
tasks.register('cleanDatagenCache') {
    doLast {
        delete 'src/main/generated/.cache'
    }
}

tasks.named('runDatagen').configure {
    dependsOn 'cleanDatagenCache'
}
configurations {
    common {
        canBeResolved = true
        canBeConsumed = false
    }
    compileClasspath.extendsFrom common
    runtimeClasspath.extendsFrom common
    developmentFabric.extendsFrom common
    shadowBundle {
        canBeResolved = true
        canBeConsumed = false
    }
}
repositories {
    maven {
        url = "https://api.modrinth.com/maven"
    }
}
dependencies {
    modImplementation "net.fabricmc:fabric-loader:$rootProject.fabric_loader_version"
    modImplementation "net.fabricmc.fabric-api:fabric-api:$rootProject.fabric_api_version"
    modImplementation "dev.architectury:architectury-fabric:$rootProject.architectury_api_version"

    modImplementation "maven.modrinth:9s6osm5g:HpMb5wGb"
    modImplementation "maven.modrinth:nvQzSEkH:5Sbkzz4O"
    modImplementation "maven.modrinth:fRiHVvU7:on5GT1qh"
    modImplementation "maven.modrinth:AANobbMI:lYTPBqRz"
    //modImplementation "maven.modrinth:YL57xq9U:bAo1Qhte"
    modImplementation "maven.modrinth:aC3cM3Vq:ylmBQ38A"
    modImplementation "maven.modrinth:EsAfCjCV:b5ZiCjAr"
    //modImplementation "maven.modrinth:yD9qW65f:TVCIPnHn"
    //modImplementation "maven.modrinth:kkmrDlKT:XNtIBXyQ"

    modImplementation "maven.modrinth:platform:BbIdNbF1"
    modImplementation "maven.modrinth:vanillabackport:ayvoY8lx"

    common(project(path: ':common', configuration: 'namedElements')) { transitive = false }
    shadowBundle project(path: ':common', configuration: 'transformProductionFabric')
}
processResources {
    inputs.property 'version', project.version
    filesMatching('fabric.mod.json') {
        expand version: inputs.properties.version
    }
}
shadowJar {
    //exclude "architectury.common.json"
    configurations = [project.configurations.shadowBundle]
    archiveClassifier = 'dev-shadow'
}
remapJar {
    inputFile.set shadowJar.archiveFile
}

sourceSets {
    main {
        java {
        }
    }
}
